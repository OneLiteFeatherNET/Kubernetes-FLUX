apiVersion: 1
groups:
  - orgId: 1
    name: core_services
    folder: Core Services
    interval: 60s
    rules:
      - uid: flux-core-layer-not-ready
        title: Flux core layer not ready
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 1200
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Sustain check lives in PromQL instead of `for:`, so short reconcile blips never
              # fire; aggregating to exactly 2 labels is a hard requirement of the Discord template.
              expr: min_over_time((max by (exported_namespace, name) (gotk_resource_info{customresource_kind="Kustomization", ready!="True"}) or max by (exported_namespace, name) (gotk_resource_info{customresource_kind="Kustomization"}) * 0)[15m:1m])
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 0s
        keepFiringFor: 15m
        annotations:
          summary: "Flux Kustomization {{ $labels.name }} (namespace {{ $labels.exported_namespace }}) has not been Ready for 15 minutes."
          problem: "Kustomization not Ready for >15 min"
          service: "Flux"
          threshold: "Ready = True (sustained 15 min)"
          object_label: "Layer"
          object_key: "name"
          location_key: "exported_namespace"
          check: "Look at the layer's status conditions first - a layer below a stuck dependency only reports that its dependency is not ready and is not the cause."
          check_command: "flux get kustomization -A"
          dashboard_url: "https://grafana.apps.onelite.feather/d/flux-control-plane"
        labels:
          severity: critical
      - uid: core-infra-helmrelease-not-ready
        title: Core infrastructure HelmRelease not ready
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 1200
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Dropped metallb/dragonfly (never existed in 90d retention), added 9 real infra
              # releases; descheduler stays out on purpose (optimiser, not a dependency).
              expr: min_over_time((max by (exported_namespace, name) (gotk_resource_info{customresource_kind="HelmRelease", ready!="True", name=~"alloy-logs|alloy-metrics|alloy-receiver|ceph-csi-drivers|cert-manager|cloudflare-tunnel-ingress-controller|cnpg|envoy|grafana|kube-prometheus-stack|loki|mariadb-operator|mariadb-operator-crds|mimir|rook-ceph|spegel|step-ca|step-issuer|tempo"}) or max by (exported_namespace, name) (gotk_resource_info{customresource_kind="HelmRelease", name=~"alloy-logs|alloy-metrics|alloy-receiver|ceph-csi-drivers|cert-manager|cloudflare-tunnel-ingress-controller|cnpg|envoy|grafana|kube-prometheus-stack|loki|mariadb-operator|mariadb-operator-crds|mimir|rook-ceph|spegel|step-ca|step-issuer|tempo"}) * 0)[15m:1m])
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 0s
        keepFiringFor: 15m
        annotations:
          summary: "Flux HelmRelease {{ $labels.name }} (namespace {{ $labels.exported_namespace }}) has not been Ready for 15 minutes."
          problem: "HelmRelease not Ready for >15 min"
          service: "Flux"
          threshold: "Ready = True (sustained 15 min)"
          object_label: "Release"
          object_key: "name"
          location_key: "exported_namespace"
          check: "Read the HelmRelease status; a failed upgrade usually leaves a pending-upgrade lock that has to be cleared before Flux retries."
          check_command: "flux get helmrelease -A"
          dashboard_url: "https://grafana.apps.onelite.feather/d/flux-control-plane"
        labels:
          severity: critical
  - orgId: 1
    name: backups
    folder: Backups
    interval: 60s
    rules:
      - uid: cnpg-backup-stale
        title: CNPG backup stale
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # cnpg_collector_last_available_backup_timestamp never updates for
              # plugin-based (barman-cloud) backups -- confirmed live it stays
              # stuck at 0 even after real completed backups. cnpg_backup_stopped_at_seconds
              # (kube-state-metrics customResourceState reading the Backup CR itself)
              # is the metric that actually reflects reality.
              expr: (time() - max(cnpg_backup_stopped_at_seconds{cluster="feather-core-cluster-pg", phase="completed"})) / 3600
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 24
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "No successful CloudNativePG (Postgres) backup in over 24h."
          problem: "No backup for over 24 h"
          service: "CloudNativePG"
          threshold: "< 24 h since last backup"
          value_unit: "h since last backup"
          value_fmt: "%.1f"
          location: "cnpg-system"
          check: "Check whether the scheduled backup still runs and whether the S3 target accepts writes."
          check_command: "kubectl -n cnpg-system get backups.postgresql.cnpg.io"
          dashboard_url: "https://grafana.apps.onelite.feather/d/cloudnative-pg"
        labels:
          severity: warning
      - uid: cnpg-backup-failing
        title: CNPG backup failing
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Same dead-metric issue as cnpg-backup-stale: the legacy
              # cnpg_collector_last_failed_backup_timestamp metric never fires
              # for plugin-based backups. Failed backups never get a stoppedAt
              # (only completed ones do), so use createdAt instead: any Backup
              # CR that entered phase=failed within the staleness window (24h).
              expr: count(cnpg_backup_created_at_seconds{cluster="feather-core-cluster-pg", phase="failed"} > (time() - 86400)) OR vector(0)
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "CloudNativePG's most recent backup attempt failed (a failure timestamp is newer than the last successful one)."
          problem: "Backup attempt failed"
          service: "CloudNativePG"
          threshold: "0 failures in 24 h"
          value_unit: "failed backups in 24 h"
          value_fmt: "%.0f"
          location: "cnpg-system"
          check: "Read the failed Backup object's error field; S3 credentials and RGW user permissions are the usual cause."
          check_command: "kubectl -n cnpg-system get backups.postgresql.cnpg.io"
          dashboard_url: "https://grafana.apps.onelite.feather/d/cloudnative-pg"
        labels:
          severity: warning
      - uid: mariadb-galera-backup-job-failed
        title: MariaDB Galera backup job failed
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: sum(kube_job_status_failed{namespace="mariadb-galera"})
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          # No dashboard_url: the mariadb-galera dashboard (gnetId 13106) covers
          # cluster health, not backup jobs, so it wouldn't add useful context here.
          summary: "The mariadb-galera-backup CronJob's most recent Job run failed."
          problem: "Backup CronJob failed"
          service: "MariaDB Galera"
          threshold: "0 failed jobs"
          value_unit: "failed jobs"
          value_fmt: "%.0f"
          location: "mariadb-galera"
          check: "Read the log of the failed Job's pod."
          check_command: "kubectl -n mariadb-galera get jobs"
        labels:
          severity: warning
  - orgId: 1
    name: storage
    folder: Storage
    interval: 60s
    rules:
      - uid: mariadb-galera-pvc-usage-high
        title: MariaDB Galera PVC usage high
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Aggregating to (persistentvolumeclaim, node) is what the Discord template needs and keeps a pod reschedule (new instance label) from spawning a second alert instance.
              expr: max by (persistentvolumeclaim, node) (kubelet_volume_stats_used_bytes{namespace="mariadb-galera", persistentvolumeclaim=~"storage-mariadb-galera-.*"} / kubelet_volume_stats_capacity_bytes{namespace="mariadb-galera", persistentvolumeclaim=~"storage-mariadb-galera-.*"}) * 100
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 80
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 10m
        annotations:
          summary: "MariaDB Galera PVC {{ $labels.persistentvolumeclaim }} on node {{ $labels.node }} is above 80% capacity."
          problem: "PVC over 80 % full"
          service: "MariaDB Galera"
          threshold: "< 80 % used"
          value_unit: "% used"
          value_fmt: "%.1f"
          object_label: "PVC"
          object_key: "persistentvolumeclaim"
          location_key: "node"
          check: "Check binlog and tmp growth before resizing; the volume grows via storage.size in the MariaDB CR."
          check_command: "kubectl -n mariadb-galera get pvc"
          dashboard_url: "https://grafana.apps.onelite.feather/d/pXgz0qFGk/galera-mariadb-overview"
        labels:
          severity: warning
      - uid: mariadb-galera-pvc-usage-critical
        title: MariaDB Galera PVC almost full
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: max by (persistentvolumeclaim, node) (kubelet_volume_stats_used_bytes{namespace="mariadb-galera", persistentvolumeclaim=~"storage-mariadb-galera-.*"} / kubelet_volume_stats_capacity_bytes{namespace="mariadb-galera", persistentvolumeclaim=~"storage-mariadb-galera-.*"}) * 100
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 90
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "MariaDB Galera PVC {{ $labels.persistentvolumeclaim }} on node {{ $labels.node }} is above 90% capacity and at risk of filling up."
          problem: "PVC over 90 % full"
          service: "MariaDB Galera"
          threshold: "< 90 % used"
          value_unit: "% used"
          value_fmt: "%.1f"
          object_label: "PVC"
          object_key: "persistentvolumeclaim"
          location_key: "node"
          check: "Check binlog and tmp growth before resizing; the volume grows via storage.size in the MariaDB CR."
          check_command: "kubectl -n mariadb-galera get pvc"
          dashboard_url: "https://grafana.apps.onelite.feather/d/pXgz0qFGk/galera-mariadb-overview"
        labels:
          severity: critical
      - uid: ceph-cluster-health-warning
        title: Ceph cluster health degraded
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: max(ceph_health_status)
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        # Only rule kept as the sentinel for "Ceph telemetry is gone": ceph_health_status was
        # present 40319/40321 min in 28 days (longest gap 1 min), so for: 10m cannot blip-fire.
        noDataState: Alerting
        execErrState: Alerting
        for: 10m
        annotations:
          summary: "Ceph cluster health is not HEALTH_OK (HEALTH_WARN or worse)."
          problem: "Ceph not HEALTH_OK"
          service: "Rook-Ceph"
          threshold: "HEALTH_OK"
          value_unit: "(0 = OK, 1 = WARN, 2 = ERR)"
          value_fmt: "%.0f"
          location: "rook-ceph-fr01"
          check: "Read the health detail output; a single failing OSD or a stuck PG is the common case."
          check_command: "kubectl -n rook-ceph-fr01 exec deploy/rook-ceph-tools -- ceph health detail"
          dashboard_url: "https://grafana.apps.onelite.feather/d/tbO9LAiZK/ceph-cluster"
        labels:
          severity: warning
      - uid: ceph-cluster-health-critical
        title: Ceph cluster health critical
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: max(ceph_health_status)
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 1
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "Ceph cluster health is HEALTH_ERR."
          problem: "Ceph HEALTH_ERR"
          service: "Rook-Ceph"
          threshold: "not HEALTH_ERR"
          value_unit: "(0 = OK, 1 = WARN, 2 = ERR)"
          value_fmt: "%.0f"
          location: "rook-ceph-fr01"
          check: "Read the health detail output; HEALTH_ERR usually means unfound objects, full OSDs or a down monitor quorum."
          check_command: "kubectl -n rook-ceph-fr01 exec deploy/rook-ceph-tools -- ceph health detail"
          dashboard_url: "https://grafana.apps.onelite.feather/d/tbO9LAiZK/ceph-cluster"
        labels:
          severity: critical
      - uid: ceph-rgw-failed-request-rate
        title: Ceph RGW failed request rate elevated
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: sum(rate(ceph_rgw_failed_req[5m]))
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 5
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 10m
        annotations:
          # Baseline observed 2026-07-18 was 0.02-0.3 req/s; a real RGW degradation
          # incident (Rook 1.20 upgrade, S3 access broken for non-owner users) would
          # show as a sustained multi-request/s error rate, not baseline noise.
          summary: "RGW failed request rate is elevated (baseline is <1/s). This is the class of failure that broke S3 access for backups/Loki/Mimir/Postgres WAL during the Rook 1.20 upgrade incident."
          problem: "RGW failure rate elevated"
          service: "Rook-Ceph RGW"
          threshold: "< 5 failed req/s"
          value_unit: "failed req/s"
          value_fmt: "%.2f"
          location: "rook-ceph-fr01"
          check: "Read the RGW log for the failing status code; 403 on named (non-owner) users is the known Rook 1.20 regression."
          check_command: "kubectl -n rook-ceph-fr01 logs -l app=rook-ceph-rgw --tail=200"
          dashboard_url: "https://grafana.apps.onelite.feather/d/tbO9LAiZK/ceph-cluster"
        labels:
          severity: warning
      - uid: ceph-osd-usage-high
        title: Ceph OSD usage high
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Per-OSD, not cluster-aggregate: the 2026-07-26 InsufficientCapacity
              # incident had individual OSDs at 94% (backfillfull) while the cluster-wide
              # average still read 86.7% -- ceph-cluster-health-warning only fires once
              # Ceph itself declares HEALTH_WARN (nearfull/backfillfull already tripped),
              # this is the earlier leading indicator on the single most-full OSD.
              expr: max(ceph_osd_stat_bytes_used / ceph_osd_stat_bytes) * 100
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 80
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 10m
        annotations:
          summary: "The most-full Ceph OSD is above 80% used. Ceph's own nearfull/backfillfull thresholds trip around here and will start rejecting large S3 uploads (InsufficientCapacity) before HEALTH_WARN fires."
          problem: "OSD over 80 % full"
          service: "Rook-Ceph"
          threshold: "< 80 % on the fullest OSD"
          value_unit: "% on the fullest OSD"
          value_fmt: "%.1f"
          location: "rook-ceph-fr01"
          check: "Compare per-OSD usage; an imbalance is reweighted, a uniformly full cluster needs capacity."
          check_command: "kubectl -n rook-ceph-fr01 exec deploy/rook-ceph-tools -- ceph osd df"
          dashboard_url: "https://grafana.apps.onelite.feather/d/tbO9LAiZK/ceph-cluster"
        labels:
          severity: warning
      - uid: ceph-osd-usage-critical
        title: Ceph OSD usage critical
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: max(ceph_osd_stat_bytes_used / ceph_osd_stat_bytes) * 100
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 90
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "The most-full Ceph OSD is above 90% used -- at or past Ceph's backfillfull threshold, new writes to affected pools (including S3/RGW uploads) will start being rejected."
          problem: "OSD over 90 % full"
          service: "Rook-Ceph"
          threshold: "< 90 % on the fullest OSD"
          value_unit: "% on the fullest OSD"
          value_fmt: "%.1f"
          location: "rook-ceph-fr01"
          check: "At this level writes to affected pools are already being rejected - free space or add capacity, reweighting alone is unlikely to be enough."
          check_command: "kubectl -n rook-ceph-fr01 exec deploy/rook-ceph-tools -- ceph osd df"
          dashboard_url: "https://grafana.apps.onelite.feather/d/tbO9LAiZK/ceph-cluster"
        labels:
          severity: critical
  - orgId: 1
    name: databases
    folder: Databases
    interval: 60s
    rules:
      - uid: cnpg-connection-saturation-warning
        title: CNPG Postgres connection saturation high
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: sum(cnpg_backends_total{namespace="cnpg-system"}) / scalar(max(cnpg_pg_settings_setting{namespace="cnpg-system", name="max_connections"})) * 100
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 80
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 10m
        annotations:
          # Threshold is against the live max_connections setting (queried dynamically),
          # not a hardcoded value - stays correct if the setting is changed again.
          summary: "CNPG Postgres connections are above 80% of max_connections. Idle pools from Harbor/Dependency-Track have starved other consumers (e.g. outline-collab) before."
          problem: "Postgres connections over 80 %"
          service: "CloudNativePG"
          threshold: "< 80 % of max_connections"
          value_unit: "% of max_connections"
          value_fmt: "%.1f"
          location: "cnpg-system"
          check: "Find the database with the largest idle pool; Harbor and Dependency-Track hold connections they do not use."
          check_command: "kubectl -n cnpg-system exec feather-core-cluster-pg-1 -- psql -Atc \"select datname,count(*) from pg_stat_activity group by 1 order by 2 desc\""
          dashboard_url: "https://grafana.apps.onelite.feather/d/cloudnative-pg"
        labels:
          severity: warning
      - uid: cnpg-connection-saturation-critical
        title: CNPG Postgres connection saturation critical
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: sum(cnpg_backends_total{namespace="cnpg-system"}) / scalar(max(cnpg_pg_settings_setting{namespace="cnpg-system", name="max_connections"})) * 100
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 90
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "CNPG Postgres connections are above 90% of max_connections - close to exhausting the shared pool."
          problem: "Postgres connections over 90 %"
          service: "CloudNativePG"
          threshold: "< 90 % of max_connections"
          value_unit: "% of max_connections"
          value_fmt: "%.1f"
          location: "cnpg-system"
          check: "Find the database with the largest idle pool and cap it; a restart storm at this level starves every other consumer."
          check_command: "kubectl -n cnpg-system exec feather-core-cluster-pg-1 -- psql -Atc \"select datname,count(*) from pg_stat_activity group by 1 order by 2 desc\""
          dashboard_url: "https://grafana.apps.onelite.feather/d/cloudnative-pg"
        labels:
          severity: critical
      - uid: mariadb-galera-cluster-degraded
        title: MariaDB Galera cluster degraded
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: min(mysql_global_status_wsrep_cluster_size{namespace="mariadb-galera"})
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 3
                    type: lt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: Alerting
        execErrState: Alerting
        # In 28 days wsrep_cluster_size was below 3 for 7 minutes total, in exactly one
        # episode >=2m and none >=5m - so 2m paged monthly for a rolling restart.
        for: 5m
        annotations:
          # min() across all 3 nodes' own view of cluster size - a node that loses
          # connectivity to its peers (e.g. the Flannel host-gw/MTU incident) reports a
          # smaller cluster size on its own, so this catches split-brain, not just a
          # fully-down node.
          summary: "MariaDB Galera cluster size has dropped below 3 - the cluster is degraded or split-brained."
          problem: "Galera cluster below 3 nodes"
          service: "MariaDB Galera"
          threshold: "3 nodes"
          value_unit: "nodes in the cluster"
          value_fmt: "%.0f"
          location: "mariadb-galera"
          check: "Check wsrep_cluster_size and wsrep_cluster_status on each node; a node that reports a different cluster UUID is split-brained."
          check_command: "kubectl -n mariadb-galera exec mariadb-galera-0 -- mariadb -e \"show status like 'wsrep_cluster%'\""
          dashboard_url: "https://grafana.apps.onelite.feather/d/pXgz0qFGk/galera-mariadb-overview"
        labels:
          severity: critical
  - orgId: 1
    name: cluster_health
    folder: Core Services
    interval: 60s
    rules:
      - uid: pods-stuck-terminating
        title: Pods stuck in Terminating
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # "OR vector(0)" always yields a series, so the noDataState path is unreachable here.
              expr: count(time() - kube_pod_deletion_timestamp > 900) OR vector(0)
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "One or more pods have been stuck in Terminating for over 15 minutes. Matches the containerd shim task.Delete hang on SIGKILL teardown seen before."
          problem: "Pods stuck in Terminating"
          service: "Kubernetes"
          threshold: "0 pods stuck > 15 min"
          value_unit: "pods stuck"
          value_fmt: "%.0f"
          check: "Check the containerd shim on the affected node - a task.Delete hang on SIGKILL teardown holds the pod forever."
          check_command: "kubectl get pods -A | grep Terminating"
        labels:
          severity: warning
  - orgId: 1
    name: observability
    folder: Observability
    interval: 60s
    rules:
      - uid: observability-pipeline-no-data
        title: Observability pipeline producing no data
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Do NOT use absent() here: it returns empty when healthy, which is the
              # opposite of what noDataState: Alerting needs. count() relies on NoData.
              expr: count(up{job="kubelet", metrics_path="/metrics"})
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 1
                    type: lt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: Alerting
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "No kubelet metrics are reaching Mimir at all - the Alloy scrape/remote-write pipeline is likely down. Every other alert in this cluster depends on this pipeline and will read as falsely healthy while this fires."
          problem: "No kubelet metrics in Mimir"
          service: "Alloy / Mimir"
          threshold: ">= 1 kubelet target"
          value_unit: "kubelet targets"
          value_fmt: "%.0f"
          location: "grafana"
          check: "Every other alert reads as falsely healthy while this fires - treat it first. Check alloy-metrics scrape and remote-write."
          check_command: "kubectl -n grafana get pods -l app.kubernetes.io/name=alloy-metrics"
        labels:
          severity: critical
      - uid: kube-state-metrics-down
        title: kube-state-metrics producing no data
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Same count()/NoData pattern as observability-pipeline-no-data above, not
              # absent(): absent() is empty while healthy, which reads as NoData forever.
              expr: count(up{job="kube-state-metrics"})
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 1
                    type: lt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: Alerting
        execErrState: Alerting
        for: 5m
        annotations:
          # No dashboard_url: the Kubernetes Objects dashboard is built from the same
          # metrics, so it is empty exactly while this alert fires.
          summary: "kube-state-metrics has stopped producing metrics. Both Flux alerts read gotk_resource_info from it, so they report healthy while blind for as long as this fires."
          problem: "No kube-state-metrics data"
          service: "kube-state-metrics"
          threshold: ">= 1 target up"
          value_unit: "targets up"
          value_fmt: "%.0f"
          location: "monitoring"
          check: "Both Flux alerts go blind while this fires. Check the kube-state-metrics pod and its ServiceMonitor."
          check_command: "kubectl -n monitoring get pods -l app.kubernetes.io/name=kube-state-metrics"
        labels:
          severity: critical
      - uid: alert-notifications-failing
        title: Alert notifications failing
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Both counters are registered WithSkipZeroValueMetrics, so they do not exist
              # at all until they were once >0 - a bare sum() would read as NoData instead
              # of 0. Hence "or vector(0)" per counter. Both are summed because the webhook
              # notifier failure path may bump either one.
              expr: (sum(increase(grafana_alerting_notification_requests_failed_total[10m])) or vector(0)) + (sum(increase(grafana_alerting_notifications_failed_total[10m])) or vector(0))
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        # "or vector(0)" always yields a value while Mimir is reachable, so NoData here only
        # means the datasource itself is down - already covered by observability-pipeline-no-data,
        # and firing this rule on it would falsely claim notifications are broken.
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "Grafana failed to deliver alert notifications, so Discord messages are being dropped - the webhook notifier does not retry. Alerts themselves keep evaluating and stay visible in the Grafana alerting UI, so use it as the source of truth until this clears."
          problem: "Discord delivery failing"
          service: "Grafana Alerting"
          threshold: "0 failures in 10 min"
          value_unit: "failed deliveries"
          value_fmt: "%.0f"
          location: "grafana"
          check: "The webhook notifier does not retry, so these messages are gone. Use the Grafana alerting UI as the source of truth until this clears."
          check_command: "kubectl -n grafana logs -l app.kubernetes.io/name=grafana --tail=200 | grep -i \"Notify for alerts failed\""
          runbook_url: "https://outline.onelitefeather.dev/doc/explanation-betriebsfallen-im-feather-core-cluster-qgL9Dgd4Ra"
        labels:
          severity: critical
      - uid: alerting-provisioning-failing
        title: Alerting provisioning failing
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: loki
            model:
              editorMode: code
              queryType: instant
              # Two signatures: the sidecar reload path (HTTP 500) and the fatal startup
              # path. sum() collapses both replicas into one message.
              expr: 'sum(count_over_time({namespace="grafana", container="grafana"} |~ "Failed to provision alerting|provisioning/alerting/reload.*status=500" [10m]))'
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        # Inverted from the metric rules: a log-based query returns NOTHING while healthy, so
        # NoData is the good case and must not fire. execErrState stays Alerting - if Loki
        # itself errors we are blind to this failure and want to hear about it.
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "Grafana rejected the provisioned alerting configuration. Alerting comes from a sidecar-collected ConfigMap and is applied through the reload endpoint, so a bad file is an HTTP 500 instead of a crash: Grafana keeps serving the last configuration it accepted, and contact points, templates, notification policies and rules silently no longer match git."
          problem: "Alerting provisioning rejected"
          service: "Grafana Alerting"
          threshold: "0 errors in 10 min"
          value_unit: "provisioning errors"
          value_fmt: "%.0f"
          location: "grafana"
          check: "Grafana keeps serving the last accepted configuration, so contact points, templates and rules silently no longer match git."
          check_command: "kubectl -n grafana logs -l app.kubernetes.io/name=grafana --tail=200 | grep -i \"provisioning/alerting/reload\""
          runbook_url: "https://outline.onelitefeather.dev/doc/explanation-betriebsfallen-im-feather-core-cluster-qgL9Dgd4Ra"
        labels:
          severity: critical
  - orgId: 1
    name: node_health
    folder: Core Services
    interval: 60s
    rules:
      - uid: node-not-ready
        title: Node not Ready
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Always has data, so noDataState: OK is correct. Do NOT write
              # `{status="true"} == 0` — the filtered value is 0 and gt 0 never fires.
              expr: max by (node) (kube_node_status_condition{condition="Ready", status=~"false|unknown"})
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "Node {{ $labels.node }} is NotReady or Unreachable."
          problem: "Node NotReady/Unreachable"
          service: "Kubernetes Node"
          threshold: "Ready = True"
          object_label: "Node"
          object_key: "node"
          check: "Check the node's conditions and, if it is unreachable, whether the machine itself is up."
          check_command: "kubectl get nodes -o wide"
        labels:
          severity: critical
      - uid: kubelet-down
        title: Kubelet down
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # min by (node) keeps a real 0/1 value per node instead of
              # filtering healthy nodes out of the result, so the series
              # never disappears while the scrape pipeline is alive. Total
              # pipeline loss is already covered by observability-pipeline-no-data.
              expr: min by (node) (up{job="kubelet", metrics_path="/metrics"})
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 1
                    type: lt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 5m
        annotations:
          summary: "Kubelet on node {{ $labels.node }} is not being scraped - the node is down, the kubelet is dead, or its serving cert expired."
          problem: "Kubelet not being scraped"
          service: "Kubelet"
          threshold: "scrape up = 1"
          object_label: "Node"
          object_key: "node"
          check: "The node is down, the kubelet is dead, or its serving certificate expired - check the certificate before rebooting anything."
          check_command: "kubectl get nodes -o wide"
        labels:
          severity: critical
      - uid: node-clock-unsynchronised
        title: Node clock not synchronised
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # machine.time.servers is /dev/ptp0 only (talos repo:
              # clusters/feather-core/talos/patches/common/time-ptp.yaml) and
              # Talos makes PTP and NTP mutually exclusive - there is NO NTP
              # fallback. A live-migration onto a host without ptp_kvm leaves
              # the clock free-running silently. node-exporter series carry
              # instance (IP:9100), not node.
              expr: min by (instance) (node_timex_sync_status)
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 1
                    type: lt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 15m
        annotations:
          summary: "Clock on {{ $labels.instance }} is not synchronised - /dev/ptp0 is the only time source and there is no NTP fallback."
          problem: "Clock not synchronised"
          service: "Node clock (PTP)"
          threshold: "sync status = 1"
          object_label: "Node"
          object_key: "instance"
          check: "/dev/ptp0 is the only time source and there is no NTP fallback, so an unsynchronised node stays unsynchronised."
          check_command: "talosctl -n <node> time"
        labels:
          severity: warning
      - uid: node-clock-offset-high
        title: Node clock offset high
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # Current cluster-wide max is 5.7 microseconds; 50ms is ~4
              # orders of magnitude above that and well below the point
              # where Galera/etcd/TLS start misbehaving.
              expr: max by (instance) (abs(node_timex_offset_seconds))
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 0.05
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 15m
        annotations:
          summary: "Clock offset on {{ $labels.instance }} exceeds 50ms. Galera certification, etcd leases and TLS validity all assume a tight cluster clock."
          problem: "Clock offset over 50 ms"
          service: "Node clock (PTP)"
          threshold: "< 50 ms offset"
          value_unit: "s offset"
          value_fmt: "%.3f"
          object_label: "Node"
          object_key: "instance"
          check: "Galera certification, etcd leases and TLS validity all assume a tight cluster clock."
          check_command: "talosctl -n <node> time"
        labels:
          severity: warning
  - orgId: 1
    name: certificates
    folder: Core Services
    interval: 60s
    rules:
      - uid: certificate-expiring-soon
        title: Certificate expiring within 14 days
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              # cert-manager's own metric. The certificate's namespace is in
              # exported_namespace (namespace= is cert-manager, where the
              # controller runs). Lowest value across the 20 series today is
              # ~72.7 days, so this will not fire on merge.
              # noDataState MUST stay OK: if a Certificate is deleted its
              # series vanishes, and "no certs" is not an emergency.
              expr: min by (exported_namespace, name) ((certmanager_certificate_expiration_timestamp_seconds - time()) / 86400)
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 14
                    type: lt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 1h
        annotations:
          summary: "Certificate {{ $labels.name }} in namespace {{ $labels.exported_namespace }} expires in under 14 days and has not renewed."
          problem: "Certificate expires in <14 days"
          service: "cert-manager"
          threshold: "> 14 days left"
          value_unit: "days left"
          value_fmt: "%.1f"
          object_label: "Certificate"
          object_key: "name"
          location_key: "exported_namespace"
          check: "Check the Certificate's conditions and the issuer; a renewal that has not started points at the issuer, not the certificate."
          check_command: "kubectl get certificate -A | grep -v True"
        labels:
          severity: warning
      - uid: certificate-expiring-critical
        title: Certificate expiring within 3 days
        condition: B
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: mimir
            model:
              editorMode: code
              expr: min by (exported_namespace, name) ((certmanager_certificate_expiration_timestamp_seconds - time()) / 86400)
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              range: false
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: '-100'
            model:
              conditions:
                - evaluator:
                    params:
                      - 3
                    type: lt
                  operator:
                    type: and
                  query:
                    params:
                      - B
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: '-100'
              expression: A
              intervalMs: 1000
              maxDataPoints: 43200
              refId: B
              type: threshold
        noDataState: OK
        execErrState: Alerting
        for: 15m
        annotations:
          summary: "Certificate {{ $labels.name }} in namespace {{ $labels.exported_namespace }} expires in under 3 days. Renewal has failed."
          problem: "Certificate expires in <3 days"
          service: "cert-manager"
          threshold: "> 3 days left"
          value_unit: "days left"
          value_fmt: "%.1f"
          object_label: "Certificate"
          object_key: "name"
          location_key: "exported_namespace"
          check: "Renewal has already failed - read the CertificateRequest's error before the certificate expires."
          check_command: "kubectl get certificate -A | grep -v True"
        labels:
          severity: critical
