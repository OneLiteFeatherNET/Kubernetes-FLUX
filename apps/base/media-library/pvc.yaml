apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-library
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-cephfs-fr01
  resources:
    requests:
      storage: 500Gi
