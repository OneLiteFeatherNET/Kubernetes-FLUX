apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sturnus
  namespace: sturnus
spec:
  releaseName: sturnus
  chart:
    spec:
      chart: ./charts/sturnus
      sourceRef:
        kind: GitRepository
        name: sturnus
        namespace: flux-system
  install:
    remediation:
      retries: 3
  interval: 1m0s
  # Default values
  #
  values: {}
