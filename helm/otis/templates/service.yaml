apiVersion: v1
kind: Service
metadata:
  name: {{ include "otis.fullname" . }}
  labels:
    {{- include "otis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otis.selectorLabels" . | nindent 4 }}
