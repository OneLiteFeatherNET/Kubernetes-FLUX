apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otis.fullname" . }}-test-connection"
  labels:
    {{- include "otis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
