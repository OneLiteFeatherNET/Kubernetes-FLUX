{{- if .Values.collaboration.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "outline.fullname" . }}-collaboration
  labels:
    {{- include "outline.labelsCollaboration" . | nindent 4 }}
  {{- with .Values.collaboration.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.frontend.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.collaboration.ingress.tls }}
  tls:
    {{- range .Values.collaboration.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.collaboration.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "outline.fullname" $ }}-collaboration
                port:
                  number: {{ $.Values.collaboration.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
