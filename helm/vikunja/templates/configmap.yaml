apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vikunja.fullname" . }}-config
  labels:
    {{- include "vikunja.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- toYaml .Values.config | nindent 4 }}
