apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  - release-gatewayapi.yaml
  - release-envoy.yaml
# Fallback: if a split Helm release still exceeds the 1Mi Secret limit
# for Helm release storage, comment the two releases above and
# uncomment crds.yaml (applied via Flux server-side apply, no Helm storage).
#  - crds.yaml
