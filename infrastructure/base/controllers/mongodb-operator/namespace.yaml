apiVersion: v1
kind: Namespace
metadata:
  name: mongodb
  labels:
    # Not `restricted` enforce: the operator injects an init container whose
    # security context has not been verified against it on this cluster.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
    pod-security.kubernetes.io/audit: restricted
