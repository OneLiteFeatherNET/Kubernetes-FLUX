apiVersion: k8s.mariadb.com/v1alpha1
kind: Database
metadata:
  name: cloudnet
spec:
  mariaDbRef:
    name: mariadb-galera
  characterSet: utf8
  collate: utf8_general_ci
  cleanupPolicy: Delete
  requeueInterval: 10h
  retryInterval: 30s