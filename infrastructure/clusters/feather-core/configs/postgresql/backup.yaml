apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: backup-fr01-initial
  namespace: cnpg-system
spec:
  cluster:
    name: feather-core-cluster-pg
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
    parameters:
      barmanObjectName: s3-store
